name: dashboard
description: Project Rakshak - Smart Floating Rescue Device Dashboard
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # 🛰️ Core Firebase Tools
  firebase_core: ^2.24.2
  firebase_database: ^10.3.7
  
  # 🗺️ Live Map & GPS Tools
  flutter_map: ^6.1.0
  latlong2: ^0.9.0
  url_launcher: ^6.2.2

  # 🎨 Design Icons
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  # Add your map image here if you have a local Kerala map file
  # assets:
  #   - assets/kerala_map.png